replicador:
  environment: ctr
  description: "Configuración de desarrollo para el replicador"

# ===========================================
# DATASOURCE CONFIG (MySQL 5.7 localhost)
# ===========================================
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/replicador?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: replicador
    password: replicador1
    driver-class-name: com.mysql.jdbc.Driver
  mail:
    host: mail.esyop.gob.ar
    port: 587
    username: sistemas@esyop.gob.ar
    password: Coys2025$%
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  # ===========================================
  # JPA / HIBERNATE CONFIG
  # ===========================================
  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# Mostrar parámetros de las queries (para debug)
logging:
  level:
    root: INFO
    com.example.replicacionservice: DEBUG

emails:
  values:
    recivers: lalleman@esyop.gob.ar,obraag@esyop.gob.ar,rafaelrio4@gmail.com
    sender: systemas@esyop.gob.ar

origen:
  value: Centros-Verdes
